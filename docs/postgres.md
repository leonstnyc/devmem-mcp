# Postgres

Postgres support is deferred for the preview release. The base package does not
ship a Postgres extra and does not require a database server.

When Postgres support is added, it will ship with standalone schema assets and
setup instructions in this repository.
